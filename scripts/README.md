# 脚本工具说明

本目录包含项目的各种脚本工具，按功能分类组织。

## 📁 目录结构

### build/ - 构建脚本
- `build-release.bat` - Windows 批处理构建脚本
- `build-release.ps1` - PowerShell 构建脚本
- `build-release-simple.bat` - 简化版构建脚本

### test/ - 测试脚本
- `run-tests.bat` - Windows 批处理测试脚本
- `run-tests.ps1` - PowerShell 测试脚本
- `test_service.bat` - 服务测试批处理脚本

### examples/ - 示例脚本
- `test_service.py` - Python 服务测试示例
- `test-basic-functionality.ps1` - PowerShell 基础功能测试

## 🚀 使用方法

### 构建项目
```bash
# 使用 PowerShell
.\scripts\build\build-release.ps1

# 使用批处理
.\scripts\build\build-release.bat

# 发布版本
.\scripts\build\build-release.ps1 -publish -o publish
```

### 运行测试
```bash
# 使用 PowerShell
.\scripts\test\run-tests.ps1

# 使用批处理
.\scripts\test\run-tests.bat
```

### 示例演示
```bash
# 运行基础功能测试
.\scripts\examples\test-basic-functionality.ps1

# 测试服务功能
python .\scripts\examples\test_service.py
```

## 📝 脚本说明

### 构建脚本
- 清理之前的构建输出
- 还原 NuGet 包
- 编译整个解决方案
- 可选：发布到指定目录
- 包含错误处理和日志输出

### 测试脚本
- 运行所有单元测试和集成测试
- 生成测试报告
- 验证代码覆盖率
- 支持并行测试执行

### 示例脚本
- 演示应用程序的核心功能
- 提供测试服务用例
- 包含基础功能验证

## ⚠️ 注意事项

1. **权限要求**: 所有脚本都需要管理员权限
2. **路径依赖**: 脚本假设从项目根目录执行
3. **环境检查**: 脚本会自动检查必要的依赖项
4. **错误处理**: 脚本包含完整的错误处理机制

## 🔧 自定义配置

大多数脚本支持参数自定义：

```powershell
# 自定义输出目录
.\scripts\build\build-release.ps1 -o "my-output"

# 启用详细日志
.\scripts\test\run-tests.ps1 -verbose

# 跳过清理步骤
.\scripts\build\build-release.ps1 -no-clean
```

---

*最后更新: 2025-12-20*