<Window x:Class="WinServiceManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinServiceManager - Windows服务管理器"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <ToolBar Grid.Row="0" Height="40">
            <Button Name="btnNewService" Click="BtnNewService_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕" Margin="0,0,5,0"/>
                    <TextBlock Text="新建服务"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button Name="btnStart" Click="BtnStart_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="▶️" Margin="0,0,5,0"/>
                    <TextBlock Text="启动"/>
                </StackPanel>
            </Button>
            <Button Name="btnStop" Click="BtnStop_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⏹️" Margin="0,0,5,0"/>
                    <TextBlock Text="停止"/>
                </StackPanel>
            </Button>
            <Button Name="btnRestart" Click="BtnRestart_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄" Margin="0,0,5,0"/>
                    <TextBlock Text="重启"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button Name="btnDelete" Click="BtnDelete_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🗑️" Margin="0,0,5,0"/>
                    <TextBlock Text="卸载"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button Name="btnRefresh" Click="BtnRefresh_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄" Margin="0,0,5,0"/>
                    <TextBlock Text="刷新"/>
                </StackPanel>
            </Button>
            <Button Name="btnViewLogs" Click="BtnViewLogs_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📄" Margin="0,0,5,0"/>
                    <TextBlock Text="查看日志"/>
                </StackPanel>
            </Button>
        </ToolBar>

        <!-- 主内容区 -->
        <Grid Grid.Row="1">
            <ListView Name="lvServices"
                      SelectionMode="Single"
                      GridViewColumnHeader.Click="GridViewColumnHeader_Click">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="服务名称" DisplayMemberBinding="{Binding DisplayName}" Width="200"/>
                        <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status, Converter={StaticResource ServiceStatusConverter}}" Width="100"/>
                        <GridViewColumn Header="描述" DisplayMemberBinding="{Binding Description}" Width="250"/>
                        <GridViewColumn Header="可执行文件" DisplayMemberBinding="{Binding ExecutablePath}" Width="200"/>
                        <GridViewColumn Header="创建时间" DisplayMemberBinding="{Binding CreatedAt}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2" Height="25">
            <StatusBarItem>
                <TextBlock Name="txtStatus" Text="就绪"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Name="txtServiceCount" Text="服务数: 0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>